import TransactionInfo from "../Components/TransactionInfo";

function Show() {
  return (
    <div className="Show">
      <h2>Transaction Information</h2>
      <section>
        <TransactionInfo />
      </section>
    </div>
  );
}

export default Show;